# Development and test dependencies.
# Keep v1 runtime dependencies available while the migration corpus is replayed.
-r requirements.txt

# Test collection and execution (Python 3.10+).
pytest>=8,<9

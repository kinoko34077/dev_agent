{
  "schema_version": 1,
  "evidence_kind": "compression_service_connection",
  "observed_at": "2026-09-15",
  "status": "NOT_VERIFIED",
  "client_contract": "PASS",
  "endpoint": "https://api.kinotch.workers.dev/v1/compress",
  "profile": "semantic-dense-v1",
  "threshold_unicode_code_points": 3000,
  "structural_max_input_code_points": 1000000,
  "provider_safe_context_limit_code_points": 200000,
  "normal_handoff_composition": "PASS (bounded local tests; live service remains unverified)",
  "live_smoke": {
    "status": "NOT_VERIFIED",
    "failure_category": "authentication_failure",
    "http_status": 403,
    "credential_source": "Windows Credential Manager",
    "credential_target": "kinotch-api / compression",
    "safe_edge_diagnostics_captured": true,
    "safe_edge_diagnostics": {
      "x_request_id_present": false,
      "server_header": "cloudflare",
      "cf_ray_present": true
    },
    "invalid_token_edge_probe": {
      "status": "NOT_VERIFIED",
      "token_value_recorded": false,
      "http_status": 403,
      "failure_category": "authentication_failure",
      "diagnostic_code": "http_forbidden",
      "x_request_id_present": false,
      "server_header": "cloudflare",
      "cf_ray_present": true
    },
    "raw_response_recorded": false
  },
  "checks": [
    "payload-only compression",
    "control fields remain uncompressed",
    "normal handoff composition lazily resolves explicit environment credentials or the fixed Credential Manager entry",
    "provider-safe input limit fails before HTTP request",
    "digest/character-count/prompt-version/model provenance validation",
    "bounded failure fallback without unbounded retry",
    "no redirect bearer forwarding"
  ],
  "limitation": "The operator smoke reached the fixed endpoint using the configured Credential Manager entry but received HTTP 403 authentication_failure. A separate one-shot probe with a deliberately invalid diagnostic token produced the same bounded 403 Cloudflare response and no application X-Request-ID. This points to an edge or pre-application gate, but does not prove the request reached the Gateway or compression Worker and does not diagnose the downstream Gemini key. No token, raw request, or response body was recorded; service authentication remains unverified."
}
